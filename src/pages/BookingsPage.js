import { Box } from "@mui/material";
import React from "react";

const BookingsPage = () => {
  return <Box>1. Get all bookings where userId = userId and map them here</Box>;
};

export default BookingsPage;
